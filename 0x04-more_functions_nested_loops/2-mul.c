#include "main.h"

/**
 * mul- define
 * Description: mulitplies to numbers
 * @a: character to multiply
 * @b: character to multiply
 * Return: the product
 */

int mul(int a, int b)
{
	return (a * b);
}
