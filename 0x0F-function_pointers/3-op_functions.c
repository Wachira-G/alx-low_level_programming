/* The file contain the 5 following functions (not more): */


/**
 * op_add - returns the sum of a and b.
 * @a: first int
 * @b: second int
 * Return: an int of the summation
 */
int op_add(int a, int b)
{
	return (a + b);
}

/**
 * op_sub - returns the difference of a and b.
 * @a: first int
 * @b: second int
 * Return: an int of the difference
 */
int op_sub(int a, int b)
{
	return (a - b);
}

/**
 * op_mul - returns the product of a and b.
 * @a: first int
 * @b: second int
 * Return: an int of the product
 */
int op_mul(int a, int b)
{
	return (a * b);
}

/**
 * op_div - returns the result of the division of a by b.
 * @a: first int
 * @b: second int
 * Return: an int of the division
 */
int op_div(int a, int b)
{
	return (a / b);
}

/**
 * op_mod - returns the remainder of the division of a by b.
 * @a: first int
 * @b: second int
 * Return: an int of the modulus operation
 */
int op_mod(int a, int b)
{
	return (a % b);
}
